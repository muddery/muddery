
var net_settings = {
    // default values

	// websocket's url
    wsurl: "ws://" + window.location.hostname + ":8001",

	csessid: false,
	    
    // resource's url
    resource_url: window.location.protocol + "//" + window.location.host,
};