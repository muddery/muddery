
var settings = {{
    // default values

    // websocket's url
    wsurl: "ws://" + window.location.hostname + ":{GAME_SERVER_PORT}",

    // resource's url
    resource_url: window.location.protocol + "//" + window.location.host + "/media/",

    // Encrypt secret messages in transporting messages.
    enable_encrypt: true,
}};
